#include<stdio.h>
#include<math.h>
int main()
{
int a,c=0,i,res;
printf("\nEnter a number : ");
scanf("%d",&a);
if(a<=40)
{
for(i=1;i<=a;i++)
{
res = pow(i,4);
c = c+res;
}
}
printf("%d",c);
return 0;
}
