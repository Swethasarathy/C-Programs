#include<stdio.h>
int sum(int n);
int main()
{
int num,c;
printf("\nEnter an integer : ");
scanf("%d",&num);
c = sum(num);
printf("\nThe sum of %d numbers are : %d",num,c);
return 0;
}
int sum(int n)
{
if(n==1)
return 1;
else
return n+sum(n-1);
}
