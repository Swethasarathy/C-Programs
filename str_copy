#include<stdio.h>
void main()
{
char str1[40],str2[40];
int i=0,len=0;
printf("\nEnter a string : ");
scanf("%s",str1);
while(str1[i]!='\0')
{
len++;
i++;
}
for(i=0;i<len;i++)
str2[i] = str1[i];
str2[i]='\0';
printf("\nThe copied string is : %s",str2);
}
